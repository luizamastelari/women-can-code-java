language: java
sudo: false

cache:
  directories:
  - "$HOME/.m2"

jdk:
  - oraclejdk8
  - oraclejdk9
  - openjdk8

after_success:
  - mvn cobertura:cobertura coveralls:report

branches:
  only:
  - master
